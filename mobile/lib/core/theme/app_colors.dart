import 'package:flutter/material.dart';

abstract final class AppColors {
  // Primary
  static const Color primaryRoseGold = Color(0xFFB76E79);
  static const Color primaryLight = Color(0xFFD4A0A7);
  static const Color primaryDark = Color(0xFF8E4F58);

  // Secondary
  static const Color secondaryIvory = Color(0xFFFFFFF0);

  // Accent
  static const Color accentDustyRose = Color(0xFFDCAE96);

  // Background & Surface
  static const Color background = Color(0xFFFFF9F5);
  static const Color surface = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF2D2D2D);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color textMuted = Color(0xFF9B9B9B);

  // Border
  static const Color border = Color(0xFFE8E0DB);

  // Semantic
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);

  // Overlays
  static const Color overlayLight = Color(0x1A000000);
  static const Color overlayDark = Color(0x80000000);

  // Shimmer
  static const Color shimmerBase = Color(0xFFE8E0DB);
  static const Color shimmerHighlight = Color(0xFFF5F0ED);
}
